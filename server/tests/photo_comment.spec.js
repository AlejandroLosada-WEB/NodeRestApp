const  app  = require('../../app');
const request  = require('supertest');

describe('GET /ping', () => {

    /*test('Code 200' , async () =>{
        const response = await request(app).get('/ping').send();
        console.log(response.statusCode)
        expect(response.statusCode).toBe(200);
    })

    test('Debe responder con un array' , async () =>{
        const response = await request(app).get('/peng').send();
        console.log(response.body)
        expect(response.body).toBeInstanceOf(Array);
    })*/

})

describe('POST /ping', () => {

    /*test('Should respond with 200' , async () =>{
        const response = await request(app).post('/pong').send();
        
        expect(response.body.pong).toBe('pong');
    })

    test('Debe responder con un array' , async () =>{
        const response = await request(app).post('/pung').send();
        console.log(response.body)
        expect(response.body).toBeInstanceOf(Array);
    })*/

})